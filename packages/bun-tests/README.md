# bun-tests

End-to-end style tests for `mock-extended` using Bun's test runner and `mock()` API.

## Commands

- `bun run test` - run Bun tests for this package
- `bun run typecheck` - typecheck this package only
