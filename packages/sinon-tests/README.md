# sinon-tests

End-to-end style tests for `mock-extended` using Sinon stubs/spies.

## Commands

- `bun run test` - run Bun tests using Sinon mocks/stubs
- `bun run typecheck` - typecheck this package only
