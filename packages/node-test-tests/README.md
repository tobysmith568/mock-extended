# node-test-tests

End-to-end style tests for `mock-extended` using the built-in `node:test` runner and `mock.fn()`.

## Commands

- `bun run test` - run Bun tests using `node:test` and `mock.fn()`
- `bun run typecheck` - typecheck this package only
