{
  "name": "vitest-tests",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "check": "biome check .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --config ./vitest.config.ts"
  },
  "dependencies": {
    "mock-extended": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "vitest": "^3.2.4"
  }
}
